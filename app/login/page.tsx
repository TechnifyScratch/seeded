'use client';
import { useState } from 'react';
import { browserDb } from '@/lib/browser';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
export default function Login(){const router=useRouter();const[email,setEmail]=useState('');const[password,setPassword]=useState('');const[error,setError]=useState('');const[busy,setBusy]=useState(false);return <main className="auth-page"><section className="auth-card"><Link href="/live" className="brand">Seeded<span>.</span></Link><div className="eyebrow">DEVELOPMENTAL RESEARCH</div><h1>Welcome back.</h1><p>Sign in to observe your experiments.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const db=browserDb();if(!db){setError('Supabase is not configured.');setBusy(false);return;}const result=await db.auth.signInWithPassword({email,password});if(result.error){setError(result.error.message);setBusy(false);}else {router.push('/live');router.refresh();}}}><label>Email address<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Password<input type="password" autoComplete="current-password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p role="alert" className="error">{error}</p>}<button className="primary full" disabled={busy}>{busy?'Signing in…':'Sign in'}</button></form><small>Invitation only. Contact your experiment administrator for access.</small></section></main>;}
